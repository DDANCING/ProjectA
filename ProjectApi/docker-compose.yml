version: '3.7'

services:
  postgress:
    image: bitnami/postgresql:latest
    ports: 
      - '5432:5432'
    environment:
      - POSTGRES_USER=zsudpxka
      - POSTGRES_PASSWORD=A_4ywuKlAlDlevUmNBilVplG1m9YDC4W
      - POSTGRES_DB=zsudpxka
    volumes:
      - polls_pg_data:/bitnami/postgresql

  redis:
    image: bitnami/redis:latest
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - '6379:6379'
    volumes:
      - polls_redis_data:/bitnami/redis/data

volumes:
  polls_pg_data:
  polls_redis_data:
